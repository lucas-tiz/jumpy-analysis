function force = piecewiseForce(contraction, pressure)

    persistent sf
    if isempty(sf)
        sf_struct = load('surface_fit-clean_cut_1p25D_crimp_over_tube_sleeve_345kpa.mat', 'sf'); 
        sf = sf_struct.('sf');
    end
    
    
    



end